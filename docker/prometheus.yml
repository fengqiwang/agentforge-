# AgentForge Prometheus 抓取配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'agentforge'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Docker 网络内通过服务名访问
      - targets: ['backend:8080']
        labels:
          app: 'agentforge'
